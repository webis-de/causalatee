# Core model dependencies
torch>=2.0
transformers>=4.40
pytorch-crf>=0.7.2
numpy>=1.24

# Optional: Flair contextual embeddings (embedding_source="flair")
# flair>=0.13

# Optional: BERT contextual embeddings (embedding_source="bert")
# Already covered by transformers above
